#ifndef GLOBALS_H_
#define GLOBALS_H_
#include <stdint.h>

extern uint32_t ms_count; // ms를 재는 변수이다
extern uint32_t sec_count; // sec를 재는 변수이다

//extern uint32_t count; // count변수

#endif // GLOBALS_H_